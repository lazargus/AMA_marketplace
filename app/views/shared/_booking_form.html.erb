<%= simple_form_for([@racket, @booking]) do |f| %>
  <%= f.input  :start_date, as: :string, input_html: { id: "range_start" } %>
  <%= f.input  :end_date, as: :string, input_html: { id: "range_end" } %>
  <%= f.submit class: "btn btn-primary"%>
<% end %>
